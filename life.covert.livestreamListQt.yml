app-id: life.covert.livestreamListQt
runtime: org.gnome.Platform
runtime-version: '48'
sdk: org.gnome.Sdk
command: livestream-list-qt

finish-args:
  # Display access
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  # Network access for API calls
  - --share=network
  # Desktop notifications
  - --talk-name=org.freedesktop.Notifications
  # System tray
  - --talk-name=org.kde.StatusNotifierWatcher
  - --own-name=org.kde.StatusNotifierItem-2-1
  # Open URLs in browser
  - --talk-name=org.freedesktop.portal.OpenURI
  - --talk-name=org.freedesktop.portal.Desktop
  # Keyring for token storage
  - --talk-name=org.freedesktop.secrets
  # Home directory for config (Qt version uses separate dir)
  - --filesystem=~/.config/livestream-list-qt:create
  # Autostart directory for launch on login
  - --filesystem=~/.config/autostart:create
  # Allow running host commands (streamlink, mpv)
  - --talk-name=org.freedesktop.Flatpak

build-options:
  build-args:
    - --share=network

modules:
  - name: livestream-list-qt
    buildsystem: simple
    build-commands:
      - pip3 install --prefix=${FLATPAK_DEST} --no-cache-dir .
      - install -Dm644 data/life.covert.livestreamListQt.desktop ${FLATPAK_DEST}/share/applications/life.covert.livestreamListQt.desktop
      - install -Dm644 data/life.covert.livestreamListQt.svg ${FLATPAK_DEST}/share/icons/hicolor/scalable/apps/life.covert.livestreamListQt.svg
      - install -Dm644 data/life.covert.livestreamListQt-symbolic.svg ${FLATPAK_DEST}/share/icons/hicolor/symbolic/apps/life.covert.livestreamListQt-symbolic.svg
      - install -Dm644 data/life.covert.livestreamListQt.metainfo.xml ${FLATPAK_DEST}/share/metainfo/life.covert.livestreamListQt.metainfo.xml
    sources:
      - type: dir
        path: .
